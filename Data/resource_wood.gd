extends StaticBody

export (PackedScene) var plenty = null
export (PackedScene) var midel = null
export (PackedScene) var low = null

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass

#func _process(delta):
#	# Called every frame. Delta is time since last frame.
#	# Update game logic here.
#	pass
